{
  "title": "Google Maps Scraper Input",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "searchTerms": {
      "title": "Search term(s)",
      "type": "array",
      "description": "List of search terms",
      "editor": "stringList",
      "prefill": ["hospitals"]
    },
    "location": {
      "title": "Location",
      "type": "string",
      "description": "Use only one location per run",
      "editor": "textfield",
      "prefill": "New York, USA"
    },
    "maxPlaces": {
      "title": "Number of places to extract (per search term)",
      "type": "integer",
      "description": "Maximum number of places to scrape for each search term",
      "editor": "number",
      "default": 150
    },
    "language": {
      "title": "Language",
      "type": "string",
      "description": "Select the language for Google Maps results",
      "editor": "select",
      "default": "en",
      "enum": ["en", "es", "fr", "de"],
      "enumTitles": ["English", "Spanish", "French", "German"]
    }
  },
  "required": ["searchTerms", "location"]
}
